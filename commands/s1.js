const {createCommand} = require('../utils/createCommand');
const { execute } = require('../utils/createCommand');

module.exports = {
    data:createCommand('s1','Sharo haceme dragon'),
    execute
};